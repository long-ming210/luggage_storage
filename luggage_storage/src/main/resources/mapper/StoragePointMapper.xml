<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.luggage.mapper.StoragePointMapper">

    <select id="selectPointWithMinpriceWithSlot" resultType="com.luggage.domain.vo.StoragePointListVO">
         SELECT
           sp.id,
           sp.business_hours,
           sp.name,
           sp.address,
           MIN(ls.price_per_hour) AS min_price,
           COUNT(ls.id) AS idle_slots
       FROM storage_point sp
           LEFT JOIN locker_slot ls ON sp.id = ls.point_id
       where  sp.status=1
       GROUP BY sp.id, sp.name, sp.address
    </select>
</mapper>
